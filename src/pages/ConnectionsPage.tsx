export default function ConnectionsPage() {
  return <div className="text-sm text-muted-foreground">Connections — managed per integration capability (connect/authorize/scopes/permissions/sync) — stub for P1.</div>;
}
